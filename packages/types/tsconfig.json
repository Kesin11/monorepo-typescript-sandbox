{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "./dist",                        /* Redirect output structure to the directory. */
  },
  "include": [
    "src/**/*"
  ]
}
